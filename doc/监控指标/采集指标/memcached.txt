go_gc_duration_seconds
垃圾收集周期暂停时间的摘要。

go_goroutines
当前存在的goroutine数量。

go_info
有关Go环境的信息。

go_memstats_alloc_bytes
已分配且仍在使用的字节数。

go_memstats_alloc_bytes_total
已分配的总字节数，即使已释放也是如此。

go_memstats_buck_hash_sys_bytes
分析存储桶哈希表使用的字节数。

go_memstats_frees_total
空闲总数。

go_memstats_gc_cpu_fraction
自程序启动以来，GC使用该程序的可用CPU时间的分数。

go_memstats_gc_sys_bytes
用于垃圾回收系统元数据的字节数。

go_memstats_heap_alloc_bytes
已分配且仍在使用的堆字节数。

go_memstats_heap_idle_bytes
等待使用的堆字节数。

go_memstats_heap_inuse_bytes
正在使用的堆字节数。

go_memstats_heap_objects
分配的对象数。

go_memstats_heap_released_bytes
已释放到OS的堆字节数。

go_memstats_heap_sys_bytes
从系统获取的堆字节数。

go_memstats_last_gc_time_seconds
自1970年以来的最后一次垃圾回收的秒数。

go_memstats_lookups_total
指针查找总数。

go_memstats_mallocs_total
总数malloc。

go_memstats_mcache_inuse_bytesmcache
结构正在使用的字节数。

go_memstats_mcache_sys_bytes
用于从系统获取的mcache结构的字节数。

go_memstats_mspan_inuse_bytesmspan
结构使用的字节数。

go_memstats_mspan_sys_bytes
用于从系统获取的mspan结构的字节数。

go_memstats_next_gc_bytes
下一次垃圾回收发生时的堆字节数。

go_memstats_other_sys_bytes
用于其他系统分配的字节数。

go_memstats_stack_inuse_bytes
堆栈分配器正在使用的字节数。

go_memstats_stack_sys_bytes
从系统获取的用于堆栈分配器的字节数。

go_memstats_sys_bytes
从系统获取的字节数。

go_threads
创建的OS线程数。

memcached_commands_total
按命令（获取，设置等）和状态细分的所有请求总数。

memcached_connections_listener_disabled_totalmemcached
达到其连接限制并禁用其侦听器的次数。

memcached_connections_total
自服务器开始运行以来打开的连接总数。

memcached_connections_yielded_total
由于达到memcached的-R限制而产生的正在运行的连接总数。

memcached_current_bytes
当前用于存储项目的字节数。

memcached_current_connections
当前打开的连接数。

memcached_current_items
此实例存储的当前项目数。

memcached_items_evicted_total
从缓存中删除到新项目的可用内存的有效项目总数。

memcached_items_reclaimed_total
过期条目中使用内存存储条目的总次数。

memcached_items_total
在此实例生存期内存储的项目总数。

memcached_limit_bytes
允许该服务器用于存储的字节数。

memcached_lru_crawler_enabled
是否启用了LRU搜寻器。

memcached_lru_crawler_hot_max_factor
将HOTLRU的闲置年龄设置为COLD年龄*此

memcached_lru_crawler_hot_percent
为HOTLRU保留的平板内存百分比。

memcached_lru_crawler_items_checked_totalLRU
爬网程序检查的项目总数。

memcached_lru_crawler_maintainer_thread
拆分LRU模式和后台线程。

memcached_lru_crawler_moves_to_cold_total
从HOT/WARM移到COLDLRU的项目总数。

memcached_lru_crawler_moves_to_warm_total
从“冷”移到“暖LRU”的项目总数。

memcached_lru_crawler_moves_within_lru_total
在HOT或WARMLRU内改组的项目总数。

memcached_lru_crawler_reclaimed_total
总计LRU爬网程序释放的项目总数。

memcached_lru_crawler_sleep
LRU爬网之间休眠的毫秒数。

memcached_lru_crawler_starts_total
启动LRU搜寻器的次数。

memcached_lru_crawler_to_crawl
每次运行每个平板要爬取的最大项目数。

memcached_lru_crawler_warm_max_factor
将WARMLRU的闲置年龄设置为COLD年龄*此

memcached_lru_crawler_warm_percent
为WARMLRU保留的平板内存百分比。

memcached_malloced_bytes
分配给平板页面的内存字节数。

memcached_max_connections
允许的最大客户端数。

memcached_read_bytes_total
此服务器从网络读取的字节总数。

memcached_slab_chunk_size_bytes
分配给此Slab类中每个块的字节数。

memcached_slab_chunks_free
尚未分配的块数。

memcached_slab_chunks_free_end
最后分配的页面末尾的空闲块数。

memcached_slab_chunks_per_page
此Slab类的单个页面中的块数。

memcached_slab_chunks_used
分配给项目的块数。

memcached_slab_cold_items
当前存储在COLDLRU中的项目数。

memcached_slab_commands_total
每个命令按命令（获取，设置等）和状态细分的所有请求总数。

memcached_slab_current_chunks
分配给此Slab类的块数。

memcached_slab_current_items
当前在此slab类中存储的项目数。

memcached_slab_current_pages
分配给此slab类的页面数。

memcached_slab_hot_age_seconds
HOTLRU中最旧项目的年龄。

memcached_slab_hot_items
当前存储在HOTLRU中的项目数。

memcached_slab_items_age_seconds
最旧的项目在slab类中的秒数。

memcached_slab_items_crawler_reclaimed_totalLRU
搜寻器释放的项目数。

memcached_slab_items_evicted_nonzero_total
具有显式到期时间集的项目在到期之前必须从LRU撤出的总次数。

memcached_slab_items_evicted_time_seconds
自上次访问此类的最近项以来的秒数。

memcached_slab_items_evicted_total
在项过期之前必须从LRU中逐出项的总数。

memcached_slab_items_evicted_unfetched_total
被驱逐且从未取回的项目总数。

memcached_slab_items_expired_unfetched_total
从LRU撤出的有效项的总数，这些有效项在设置后从未被触摸过。

memcached_slab_items_moves_to_cold
从HOT或WARM移到COLD的项目数。

memcached_slab_items_moves_to_warm
项目数量从“冷”移到“暖”状态。

memcached_slab_items_moves_within_lru
在HOT或WARM中碰撞活动项的次数。

memcached_slab_items_outofmemory_total
此Slab类的已触发内存不足错误的项目总数。

memcached_slab_items_reclaimed_total
回收的项目总数。

memcached_slab_items_tailrepairs_total
需要修复特定ID条目的总次数。

memcached_slab_lru_hits_total
到LRU的get_hits数。

memcached_slab_mem_requested_bytes
平板中实际项目占用的内存字节数。

memcached_slab_warm_age_seconds
HOTLRU中最旧项目的年龄。

memcached_slab_warm_items
当前存储在WARMLRU中的项目数。

memcached_time_seconds
当前UNIX时间。

memcached_up
是否可以访问memcached服务器。

memcached_uptime_seconds
服务器启动以来的秒数。

memcached_version此memcached
服务器的版本。

memcached_write_bytes_total
此服务器发送到网络的字节总数。

process_cpu_seconds_total
花费的总用户和系统CPU时间（以秒为单位）。

process_max_fds
打开文件描述符的最大数量。

process_open_fds
打开文件描述符的数量。

process_resident_memory_bytes
驻留内存大小（以字节为单位）。

process_start_time_seconds
自UNIX纪元以来的进程开始时间（以秒为单位）。

process_virtual_memory_bytes
虚拟内存大小（以字节为单位）。

process_virtual_memory_max_bytes
可用的最大虚拟内存量（以字节为单位）。

promhttp_metric_handler_requests_in_flight
当前提供的剪贴簿数量。

promhttp_metric_handler_requests_total
通过HTTP状态代码进行的刮擦总数。